<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@layout/fragment_timer">

    <fragment
        android:id="@+id/navigation_record"
        android:label="record"
        android:name="com.example.tomate.TimerFragment"
        tools:layout="@layout/fragment_studyrecord" />

    <fragment
        android:id="@+id/navigation_timer"
        android:label ="timer"
        android:name="com.example.tomate.TimerFragment"
        tools:layout="@layout/fragment_timer" />

    <fragment
        android:id="@+id/navigation_ranking"
        android:label="Ranking"
        android:name="com.example.tomate.RankingFragment"
        tools:layout="@layout/fragment_ranking" />
</navigation>